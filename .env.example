# 复制本文件为 .env，填入你的真实值。
#   cp .env.example .env
# .env 已被 git 忽略，不会上传；本 .example 不含机密，可入库当模板。
#
# 火山引擎方舟控制台：https://console.volcengine.com/ark
#   - API Key 管理  → 生成 → 填到 ARK_API_KEY（机密）
#   - 在线推理/接入点 → 创建 → 形如 ep-xxxx → 填到 ARK_MODEL

ARK_API_KEY=在此填入你的_API_Key
ARK_MODEL=ep-在此填入你的推理接入点id

# 可选，默认北京区，一般不用改：
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
