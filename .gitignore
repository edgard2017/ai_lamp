# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Secrets — real key lives here, NEVER commit it
.env
.env.local

# Runtime data (calibration baselines, sqlite db, captured frames)
data/

# Editor / OS
.DS_Store
.vscode/
